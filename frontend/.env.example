# Backend URL the frontend talks to. Defaults to http://localhost:8000 if unset.
# The packaged desktop app injects this at runtime instead (window.__RAG_API_BASE__),
# so you normally only set this for custom dev setups.
# VITE_API_BASE_URL=http://localhost:8000
