# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
venv/
env/
ENV/
.env
.venv
env.bak/
venv.bak/

# Streamlit
.streamlit/secrets.toml
# Keep config.toml if it exists
!.streamlit/config.toml

# VS Code
.vscode/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/
cover/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# RAG specific
*.faiss
*.pkl

# RAG runtime data (per-user, never commit) — uploads, vector store, SQLite
# history, downloaded models (reranker/GGUF), and local settings.
backend/uploads/
backend/vector_store/
backend/models/
backend/rag.db
backend/settings.json
# Encrypted in-app API-key store (Fernet) + its key — NEVER commit these.
keys.enc
secret.key
backend/keys.enc
backend/secret.key

# Node / frontend
node_modules/
dist/
*.local

# OS / editor cruft
.DS_Store
Thumbs.db

# graphify knowledge graph (regenerable)
graphify-out/

# ── Desktop packaging (Phase 4) — all regenerable build artifacts ──────
# PyInstaller output + work dirs (backend freeze)
backend/build_pyi/
backend/dist/
backend/*.spec.bak

# Tauri / Rust build output
frontend/src-tauri/target/
frontend/src-tauri/gen/
# Frozen backend sidecar binaries staged for bundling (large, per-platform)
frontend/src-tauri/binaries/
# Keep the icons (committed source assets), ignore nothing else under src-tauri


# Demo/transfer build output (144 MB installer + bundled data) — regenerable, never commit
DEMO_TRANSFER/
