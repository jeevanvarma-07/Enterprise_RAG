# Development / test dependencies (not needed to run the app).
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest
flashrank          # so reranking tests exercise the real path on dev machines
pyinstaller        # freezes the backend into the desktop-app sidecar (Phase 4)
